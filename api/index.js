
//
// Register route modules here
// Also, any configuration needed for routes but not the entire app happens here
//

module.exports = {
    stats: require('./stats'),
    users: require('./users'),
};
